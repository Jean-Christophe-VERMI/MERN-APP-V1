import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  notification: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    margin: '1rem',
  },

}));